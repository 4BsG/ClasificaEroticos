import { render } from '@testing-library/react';
import '@testing-library/jest-dom';
import SelectPrefix from '../SelectPrefix';

describe('SelectPrefix', () => {
  it('renders an SelectPrefix', () => {
    render(<SelectPrefix />);
  });
});
